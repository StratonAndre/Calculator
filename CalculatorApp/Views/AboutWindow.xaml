<Window x:Class="CalculatorApp.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About CalculatorApp" 
        Height="350" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" 
                   Text="CalculatorApp App" 
                   FontSize="28" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center"
                   Foreground="{DynamicResource ForegroundBrush}"/>
        
        <Separator Grid.Row="1" Margin="0,10"/>
        
        <StackPanel Grid.Row="2" Margin="0,10">
            <TextBlock Text="Created by:" 
                       FontSize="14"
                       Foreground="{DynamicResource ForegroundBrush}"/>
            <TextBlock Text="Straton Andre Antonio" 
                       FontWeight="SemiBold" 
                       FontSize="16"
                       Margin="20,5,0,0"
                       Foreground="{DynamicResource ForegroundBrush}"/>
            
            <TextBlock Text="Group:" 
                       FontSize="14"
                       Margin="0,15,0,0"
                       Foreground="{DynamicResource ForegroundBrush}"/>
            <TextBlock Text="Grupa 4" 
                       FontWeight="SemiBold" 
                       FontSize="16"
                       Margin="20,5,0,0"
                       Foreground="{DynamicResource ForegroundBrush}"/>
            
            <TextBlock Text="© by Hopes and dreams" 
                      HorizontalAlignment="Center" 
                      Margin="0,15,0,0" 
                      Foreground="{DynamicResource ForegroundBrush}"/>
        </StackPanel>
        
        <Button Grid.Row="4" 
                Content="OK" 
                Width="100" 
                Height="30"
                HorizontalAlignment="Center"
                Click="OkButton_Click"
                Style="{StaticResource ModernNumberButtonStyle}"/>
    </Grid>
</Window>